import { ValidationExceptionFilterFilter } from './validation-exception-filter.filter';

describe('ValidationExceptionFilterFilter', () => {
  it('should be defined', () => {
    expect(new ValidationExceptionFilterFilter()).toBeDefined();
  });
});
